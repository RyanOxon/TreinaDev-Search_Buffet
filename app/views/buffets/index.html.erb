<div class="content" >
  <h1>Buffets registrados</h1>
  <ul>
    <% @buffets.each do |buffet| %>
      <li>
        <dt><%= link_to buffet.brand_name, buffet %></dt>
        <dd><%= buffet.city %> | <%= buffet.state_code %> </dd>
      </li>

    <% end %>
  </ul>
</div>